name: Signing profile guard

on:
  push:
  pull_request:

jobs:
  verify-portable-profile:
    runs-on: windows-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Reject committed signing material
        shell: pwsh
        run: ./tools/check-signing-profile.ps1
